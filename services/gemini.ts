// Gemini AI features are disabled for this stand-alone version.
export async function getTutorResponse(userMessage: string, history: {role: 'user' | 'assistant', content: string}[]) {
  return "AI features are currently disabled in this version of the application.";
}